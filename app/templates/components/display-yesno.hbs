<button class="btn btn-flat" {{action 'toggleValue'}}>
  {{#if yes}}
    <i class="yesno-icon glyphicon glyphicon-ok text-success"></i>
  {{else}}
    <i class="yesno-icon glyphicon glyphicon-remove text-danger"></i>
  {{/if}}
</button>